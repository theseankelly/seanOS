#ifndef IO_H
#define IO_H

void out(unsigned short port, unsigned char data);
/* void out(unsigned short port, unsigned char data)
 * 		unsigned short port is the address to data port to write to
 * 		unsigned char data is the byte to write to that port
 * 		returns nothing
 *
 * 		Simple routine to write a byte to an output port
 */


#endif
